\begin{frame}{Laufzeit: "Uberblick}
    \vspace{-1cm} 
    \begin{overlayarea}{\textwidth}{6cm}
        \begin{algorithmic}
        \begin{tabular}[H]{rl}
            \only<2,3>{\textcolor{blue}{$O(n+m)$}}
            & 
            \STATE $G_1, C$ $\leftarrow$
                                   \begin{tabular}[H]{l}
                                       3 Bor\r uvka-Phasen\\
                                       \textbf{Wenn} $G$ leer\\
                                       \ \ \ \ \ \ \ \ oder Bor\r uvka-Phasen
                                       terminieren:\\
                                       \ \ \ \ \ \ \ \ \textbf{return} $F=C$\\
                                   \end{tabular}\\
            \\
            \only<2,3>{\textcolor{blue}{$O(n+m)$}}
            & \STATE $G_2$ $\leftarrow$ $G_1(p=0,5)$\\
            \\
            \only<3>{\textcolor{red}{?}}
            & \STATE $F_2$ $\leftarrow$ $MST(G_2)$\\
            \\
            \only<2,3>{\textcolor{blue}{$O(n+m)$}}
            & \STATE $G_3$ $\leftarrow$ $(V_{G_1}, E_{G_1} - E_{F_2-heavy})$\\
            \\
            \only<3>{\textcolor{red}{?}}
            & \STATE $F_3$ $\leftarrow$ $MST(G_3)$\\
            \\
            \only<2,3>{\textcolor{blue}{$O(n+m)$}}
            & \RETURN $F = C \cup F_3$\\
            \ \ \ \ \ \ \ \ \ \ \ \ \ \ &\\
        \end{tabular}
        \end{algorithmic}
    \end{overlayarea}
\end{frame}

\begin{frame}{Laufzeit: Rekursionsungleichung}
    \vspace{-2cm}
    \begin{overlayarea}{\textwidth}{4cm}
\setlength{\belowdisplayskip}{-5mm}
        \begin{flalign*}
            T(m,n)  &\leq \only<1,2,3>{$\textcolor{red}{?}$ }
                        \only<4,5,6,7,8,9,10,11>{$T(n/8, m/2)$}
                        + \only<1,2,3,4,5>{$\textcolor{red}{?}$ }
                          \only<6,7,8,9,10,11>{$T(n/8, n/4)$ }
                        + c(m+n)&&\\
                    \only<7>{
                        &\leq (T(n/8^2, m/2^2) + T(n/8^2, \frac{2n}{8^2}) + c(n/8+m/2))&&\\
                        & + (T(n/8^2, \frac{n/4}{2}) + T(n/8^2, \frac{2n}{8^2}) + c(n/8+n/4))&&\\
                        & + c(n+m)&&\\
                    }
                    \only<8>{
                        &\leq (T(n/8^2, m/2^2) + T(n/8^2, \frac{2n}{8^2})&&\\
                        & + T(n/8^2, \frac{n/4}{2}) + T(n/8^2, \frac{2n}{8^2})&&\\
                        & + c(n/8+n/4) + c(n/8+m/2)) + c(n+m)&&\\
                    }
                    \only<9,10,11>{
                        &\leq (T(n/8^2, m/2^2) + T(n/8^2, \frac{2n}{8^2})&&\\
                        & + T(n/8^2, \frac{n/4}{2}) + T(n/8^2, \frac{2n}{8^2})&&\\
                        & + c(n/2+m/2)) + c(n+m)&&\\
                    }
                    \only<10,11>{
                        &\leq c (n+m) \cdot \sum_{i=0}^{\infty} (1/2)^i&&\\
                    }
                    \only<11>{
                        &= 2 \cdot c(n+m)&&\\
                    }
        \end{flalign*}
        , mit $c \in \mathbb{N}$ konstant\\
        \gap
        \begin{tabular}[H]{ll}
            \only<2,3,4,5,6>{\cellcolor{GreenTOL!40!}
            $G_1$   
            &\cellcolor{GreenTOL!20!}
            $n_{G_1} = n_G/8, m_{G_1} = m_G$\\
            }\only<3,4,5,6>{\cellcolor{GreenTOL!40!}
            $G_2$   
            &\cellcolor{GreenTOL!20!}
            $n_{G_2} = n_G/8, m_{G_2} = m_G/2$\\
            }\only<5>{\cellcolor{GreenTOL!40!}
            $G_3$   
            &\cellcolor{GreenTOL!20!}
            $n_{G_3} = \frac{n_G/8}{0,5}, m_{G_3} = m_G/2$\\
            }\only<6>{\cellcolor{GreenTOL!40!}
            $G_3$   
            &\cellcolor{GreenTOL!20!}
            $n_{G_3} = n_G/4, m_{G_3} = m_G/2$\\
            }\\
        \end{tabular}
    \end{overlayarea}
\end{frame}
